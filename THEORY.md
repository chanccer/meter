# 压电定位系统控制理论

本文从物理出发，逐层解释系统中每一项技术选型的动机和原理：为什么要建 LUT、为什么用 FOPDT 建模、PID 和 ADRC 分别解决什么问题，以及各模块如何组合成完整的控制架构。

---

## 目录

1. [压电执行器的物理特性](#1-压电执行器的物理特性)
2. [系统辨识：FOPDT 模型](#2-系统辨识fopdt-模型)
3. [查找表（LUT）方法](#3-查找表lut方法)
4. [PID 控制与 IMC 整定](#4-pid-控制与-imc-整定)
5. [ADRC：自抗扰控制](#5-adrc自抗扰控制)
6. [Smith Predictor：死区补偿](#6-smith-predictor死区补偿)
7. [Bouc-Wen 迟滞模型](#7-bouc-wen-迟滞模型)
8. [整体控制架构](#8-整体控制架构)

---

## 1. 压电执行器的物理特性

### 1.1 逆压电效应

压电陶瓷（PZT）在施加电场时会产生机械形变，这称为**逆压电效应**。理想情况下，位移 $d$ 与电压 $V$ 成线性关系：

$$d = d_{33} \cdot V$$

其中 $d_{33}$ 是压电应变常数（单位 pm/V），代表沿极化方向单位电压产生的位移。对于纳米定位场景，这意味着几伏特的电压可以驱动数百至数千纳米的行程。

然而，实际压电器件远非理想线性系统，存在三类显著非线性：

### 1.2 迟滞（Hysteresis）

迟滞是压电器件最突出的非线性。同一电压对应的位移，**升压时**和**降压时**不同——两条曲线围成一个封闭的回线（迟滞环）。

**物理根源**：压电陶瓷内部存在大量铁电畴（ferroelectric domain），每个畴在外电场下可发生极化翻转。翻转过程是不可逆的能量耗散行为（类似磁滞中的磁畴翻转）。升压时铁电畴沿电场方向对齐；降压时，已翻转的畴不能完全复位，导致相同电压下位移偏高。迟滞幅度通常为行程的 10%～20%。

**后果**：如果只用一条 $V \to d$ 曲线（不区分方向），最大误差等于迟滞幅度——对于 2000 nm 行程的器件可达 200～400 nm，远超纳米定位要求。

### 1.3 蠕变（Creep）

即使电压保持不变，压电位移也会随时间缓慢漂移，这称为蠕变。

**物理根源**：铁电畴翻转后并未完全到达最终平衡态，而是在热激活下继续缓慢弛豫。这一过程可持续数秒到数分钟，位移变化量与时间的对数成正比：

$$\Delta d(t) \approx \gamma \cdot d_0 \cdot \log\!\left(1 + \frac{t}{t_0}\right)$$

**后果**：LUT 采集时若等待时间不足，记录的位移仍在漂移；控制系统锁定目标后长时间保持，实际位置也会缓慢偏移。这是当前系统使用固定 `SETTLE_TIME` 而非主动稳态检测时的主要不确定性来源。

### 1.4 容性动态与有限带宽

压电器件本质上是一个有损电容，驱动源通过有限输出阻抗对其充电。位移随时间以指数形式趋近稳态，具有有限的上升时间。在小信号近似下，这一动态可以用一阶低通环节描述，时间常数约为：

$$\tau = R_{\text{src}} \cdot C_{\text{piezo}}$$

典型值为数十毫秒量级（本系统辨识结果约 80 ms），限制了系统的动态响应带宽。

---

## 2. 系统辨识：FOPDT 模型

### 2.1 为什么选 FOPDT

完整的压电物理模型极其复杂，包含非线性迟滞、蠕变、热漂移等因素。但对于**闭环控制器设计**，我们需要的不是精确物理模型，而是一个能够捕捉**主导动态**的简化模型——足以指导增益整定即可。

**一阶加纯滞后（FOPDT）**是这一场景的标准选择：

$$G(s) = \frac{K \cdot e^{-\theta s}}{\tau s + 1}$$

| 参数 | 物理意义 | 典型值 |
|------|----------|--------|
| $K$ | 静态增益（nm/V） | ~410 nm/V |
| $\tau$ | 时间常数（机械+电气综合效应） | ~80 ms |
| $\theta$ | 纯滞后（协议延迟 + 机械延迟） | ~4 ms |

FOPDT 只有三个参数，可从单次阶跃响应中稳健辨识，且在整定 PID/ADRC 时有成熟的解析公式直接可用。更重要的是，对于低频定位任务（带宽远低于 $1/\tau$），FOPDT 已能充分描述系统的响应特征。

### 2.2 阶跃响应辨识

给定 FOPDT 模型，其阶跃响应（$t=0$ 时电压从 0 跳变至 $\Delta V$）为：

$$y(t) = \begin{cases} 0 & t < \theta \\ K \cdot \Delta V \cdot \left(1 - e^{-(t-\theta)/\tau}\right) & t \geq \theta \end{cases}$$

辨识方法：施加电压阶跃，采集位移时间序列，对上式进行非线性最小二乘拟合，同时估计 $K$、$\tau$、$\theta$。为提高鲁棒性，重复多次取平均，并剔除拟合质量差（$R^2 < 0.95$）的次数。

### 2.3 延迟的分解

纯滞后 $\theta$ 在本系统中由两部分组成：

$$\theta = \theta_{\text{protocol}} + \theta_{\text{piezo}}$$

- $\theta_{\text{protocol}}$：Moku:Go 命令执行延迟 + USB/串口帧传输延迟，通过专门的往返时间测量确定，与压电器件无关
- $\theta_{\text{piezo}}$：压电器件本身的机械延迟（畴翻转传播时间等），从总滞后中减去协议延迟得到

这一分解使得 ADRC 的 Smith Predictor 能够只补偿**不可控的机械延迟**，而无需依赖可能随温度漂移的器件参数来估计协议延迟。

---

## 3. 查找表（LUT）方法

### 3.1 LUT 的定位

LUT 是对压电**静态非线性**（主要是迟滞）的直接测量和记录。它不依赖任何模型假设，而是在全行程范围内密集采样，建立 $(T, V, \text{direction}) \to d$ 的数据驱动映射。

### 3.2 双向扫描的必要性

由于迟滞的存在，LUT 必须分别记录升压（up）和降压（down）两条曲线。如果只记录一个方向，当实际运动方向与 LUT 方向不符时，前馈误差等于该点的迟滞幅度。

在本系统中，控制器起始时读取当前位移，判断目标位移在当前位置的上方还是下方，据此选择对应的 LUT 分支进行反查：

$$V_{\text{ff}} = \text{LUT}^{-1}(T,\, d_{\text{target}},\, \text{direction})$$

### 3.3 正向与反向插值

**正向查询**：给定温度 $T$ 和电压 $V$，返回位移 $d$。实现为二维线性插值（RegularGridInterpolator），在温度轴和电压轴上分别插值。

**反向查询**：给定温度 $T$ 和目标位移 $d_{\text{target}}$，返回所需电压 $V$。正向映射 $d(V)$ 在各分支上是单调递增的，因此反向查询转化为单变量方程求根：

$$\text{LUT}(T, V, \text{direction}) - d_{\text{target}} = 0$$

使用 Brent 法（二分法与割线法的混合）求解，收敛到 $10^{-6}$ V 精度。

### 3.4 LUT 的局限性

LUT 本质上是静态映射，存在以下局限：

- **温度依赖**：压电灵敏度随温度变化，需要多温度点建表并插值
- **老化漂移**：长期使用后极化状态退化，LUT 需重新采集
- **蠕变不可见**：采集时如果稳定时间不足，LUT 记录的是瞬态值而非真正稳态
- **纯静态**：LUT 不包含动态信息，无法补偿瞬态响应，必须与反馈控制配合使用

---

## 4. PID 控制与 IMC 整定

### 4.1 为什么需要反馈

LUT 前馈将初始电压设置在目标附近，但仍有残余误差，来源包括：

- LUT 采集时的噪声（±5 nm 量级）
- 插值误差（步长 0.1 V 内的非线性）
- 温度偏差（实际温度与 LUT 采集温度不完全一致）
- 蠕变（LUT 记录的不是真实稳态值）

反馈控制器持续测量位移误差并修正电压，消除上述所有稳态误差，将最终精度提升到传感器噪声水平（±5 nm RMS）。

### 4.2 PI 控制律

采用带积分项的 PI 控制器（不加微分项，因为位移信号噪声较高）：

$$u(t) = K_p \cdot e(t) + K_i \int_0^t e(\tau)\,d\tau$$

离散化后：

$$u[k] = u[k-1] + K_p (e[k] - e[k-1]) + K_i \cdot \Delta t \cdot e[k]$$

积分项设有抗饱和限幅（±0.5 V），防止在大误差阶段积分项无限增长（积分飞车），导致超调后长时间难以复位。

### 4.3 IMC 整定（Rivera 1986）

增益 $K_p$、$K_i$ 不需要手动试凑——基于辨识得到的 FOPDT 参数，IMC（内模控制）方法给出解析整定公式。

**核心思想**：将 PID 控制器等价为带模型的内环补偿器 $Q(s)$，使得闭环传递函数的主极点位置由单一参数 $\lambda$（闭环时间常数）决定。

**有效延迟**：IMC 将纯滞后用 Padé 近似处理，定义有效延迟：

$$\theta_{\text{eff}} = \theta_{\text{plant}} + \theta_{\text{sensor}} + \frac{\Delta t}{2}$$

其中 $\Delta t / 2$ 是由于离散采样引入的半拍延迟，$\theta_{\text{sensor}}$ 是传感器传输延迟。

**整定公式**：

$$\lambda = 2\theta_{\text{eff}} \quad \text{（推荐值，兼顾响应速度与鲁棒性）}$$

$$K_p = \frac{\tau + \theta_{\text{eff}}/2}{K(\lambda + \theta_{\text{eff}}/2)}$$

$$K_i = \frac{K_p}{\tau + \theta_{\text{eff}}/2}$$

$$K_d = \frac{K_p \cdot \tau \cdot \theta_{\text{plant}}}{2\tau + \max(\theta_{\text{plant}},\, \epsilon)}$$

**物理解释**：$\lambda$ 越大，闭环越保守（响应慢但鲁棒）；$\lambda$ 越小，响应越激进，但对模型误差更敏感。公式的分母中出现 $\lambda + \theta_{\text{eff}}/2$，意味着当纯滞后增大时，增益自动降低——这正是延迟对控制性能的本质限制。

**稳定性保证**：只要 FOPDT 是对被控对象的合理近似，IMC 在任意 $\lambda > 0$ 下均能保证闭环稳定，不存在类似 Ziegler-Nichols 法那样超过稳定边界的风险。

---

## 5. ADRC：自抗扰控制

### 5.1 核心思想

PID 依赖精确的模型参数来整定增益，当温度变化、老化或蠕变使植物参数偏移时，性能退化。ADRC 的核心思想是：**不要试图精确建模所有非线性——把它们全部视为"总扰动"，实时估计并主动抵消。**

将压电系统的运动方程写成：

$$\dot{y} = f(y, u, t) + b_0 \cdot u$$

其中 $b_0 = K/\tau$ 是已知的近似增益，$f(\cdot)$ 是包含迟滞、蠕变、未建模动态、外部扰动在内的**总扰动**——不需要知道它的解析形式，只需要能实时估计它。

### 5.2 扩张状态观测器（ESO）

定义扩张状态 $z_1 = y$（位移估计）、$z_2 = f(\cdot)$（总扰动估计），则系统变为：

$$\begin{cases} \dot{z}_1 = z_2 + b_0 u \\ \dot{z}_2 = \dot{f} \approx 0 \end{cases}$$

针对此系统设计全维观测器，带宽参数 $\omega_0$（ESO 带宽）：

$$\begin{cases} \dot{\hat{z}}_1 = \hat{z}_2 + b_0 u + 2\omega_0 (y - \hat{z}_1) \\ \dot{\hat{z}}_2 = \omega_0^2 (y - \hat{z}_1) \end{cases}$$

这是一个以 $y$（实测位移）和 $u$（施加电压）为输入，输出 $\hat{z}_1$（位移估计）和 $\hat{z}_2$（总扰动估计）的动态滤波器。只要 $\omega_0$ 足够大，$\hat{z}_2$ 能快速跟踪真实总扰动 $f(\cdot)$，包括迟滞、蠕变及任何缓变外扰。

### 5.3 控制律

ADRC 控制律分两步：

**第一步**：假设扰动已被完全补偿，设计简单的比例控制：

$$u_0 = \omega_c (r - \hat{z}_1) + \dot{r}$$

其中 $\omega_c$ 是控制带宽，$\dot{r}$ 是设定值前馈微分项，提升跟踪速度。

**第二步**：将扰动估计反馈，从控制量中抵消：

$$u = \frac{u_0 - \hat{z}_2}{b_0}$$

整理后，闭环传递函数近似为 $\omega_c / (s + \omega_c)$——一个一阶低通，与植物的具体参数无关。这就是 ADRC 鲁棒性的本质：通过实时扰动补偿，将非线性时变系统"线性化"为一个仅由 $\omega_c$ 决定的积分器。

### 5.4 带宽参数的选取

ADRC 有两个核心带宽参数：

- $\omega_c$（控制带宽）：决定闭环响应速度，受限于传感器噪声和执行器饱和。对于 τ ≈ 80 ms 的压电系统，$\omega_c \approx 1/(\tau + \theta_{\text{eff}})$ 是 IMC 给出的推荐值
- $\omega_0$（ESO 带宽）：决定扰动估计的跟踪速度，通常取 $\omega_0 = 5\omega_c$。$\omega_0$ 过小则扰动估计滞后，过大则放大噪声

**两者的权衡**：$\omega_0$ 必须远大于被估计扰动的变化频率，但又不能大到将传感器噪声放大进控制量。在压电定位中，蠕变和迟滞的变化频率远低于 $\omega_0$，因此估计效果良好。

### 5.5 ZOH 精确离散化

控制循环在数字计算机上以固定步长 $\Delta t = 50$ ms 运行，ESO 的连续方程必须离散化。

**为什么不用欧拉法**：欧拉前向离散化的稳定条件是 $\omega_0 \Delta t < 2$。当 $\omega_0 = 50$ rad/s、$\Delta t = 0.05$ s 时，$\omega_0 \Delta t = 2.5 > 2$，欧拉法**直接不稳定**。

**ZOH（零阶保持）精确离散化**：将 ESO 的矩阵微分方程写成增广形式：

$$\dot{\mathbf{x}}_{\text{aug}} = M_{\text{aug}} \mathbf{x}_{\text{aug}}, \quad M_{\text{aug}} = \begin{bmatrix} A_c & B_c \\ 0 & 0 \end{bmatrix}$$

其中 $A_c$、$B_c$ 是 ESO 的连续状态矩阵。ZOH 精确离散化通过矩阵指数一次性给出精确的离散等价：

$$\begin{bmatrix} A_d & B_d \\ 0 & I \end{bmatrix} = e^{M_{\text{aug}} \Delta t}$$

$e^{M_{\text{aug}} \Delta t}$ 通过 `scipy.linalg.expm` 数值计算，结果与 MATLAB 中 `expm(Maug*DT)` 完全等价（误差 $< 10^{-10}$）。这保证了 ESO 在任意 $\omega_0$ 和 $\Delta t$ 组合下均无条件稳定，无需为稳定性降低步长或增益。

---

## 6. Smith Predictor：死区补偿

### 6.1 纯滞后对控制的影响

FOPDT 中的纯滞后 $\theta$ 是控制性能的根本限制因素。对于 PI 控制器，增益的绝对上限受奈奎斯特稳定条件约束：

$$K_p \lesssim \frac{\tau}{K \cdot \theta}$$

即滞后越大，允许的增益越小，响应越慢。纯滞后还使 ESO 的观测产生偏差——观测器看到的是 $\theta$ 时间前施加的控制量的效果，而不是当前控制量的效果。

### 6.2 Smith Predictor 的结构

Smith Predictor 在 ESO 内部并联一个 FOPDT 预测模型，用来抵消已知滞后的影响。设植物的无延迟部分为 $G_0(s) = K/(\tau s + 1)$，则 Smith 补偿器使 ESO"看到"的等效被控对象变为：

$$G_{\text{eff}}(s) = G_0(s) \cdot e^{-\theta_s s}$$

其中 $\theta_s$ 是传感器和采样引入的固有延迟（不可消除），而植物机械延迟 $\theta_{\text{plant}}$ 被预测模型抵消。这等效于让控制器在无机械延迟的系统上工作，使得可用增益显著提高。

**实现方式**：在每个控制步，同时运行一个离散 FOPDT 仿真模型（欧拉积分），并维护一个长度为 $\lfloor\theta_{\text{plant}}/\Delta t\rfloor$ 的循环缓冲区存储历史输出，再将缓冲区末端值注入 ESO 的测量输入。

### 6.3 何时开启 Smith Predictor

当 $\theta_{\text{plant}}/\tau$ 比值较大时，Smith Predictor 效益显著。对于本系统（$\theta_{\text{plant}} \approx 1$ ms，$\tau \approx 80$ ms），比值约为 1.25%，Smith 补偿的提升有限，但对于延迟更大的器件（如通过长电缆驱动的压电，或热控场景），则相当重要。

---

## 7. Bouc-Wen 迟滞模型

### 7.1 用途定位

Bouc-Wen 模型在本系统中**仅用于仿真**（`sim/` 目录下的 MATLAB 代码），不参与实际控制循环。它的作用是：在仿真中引入更真实的非线性迟滞，验证 ADRC 在有迟滞扰动时的鲁棒性，而不是像 FOPDT 那样假设系统是线性的。

### 7.2 数学模型

Bouc-Wen 模型通过引入一个辅助状态变量 $z$（迟滞内部状态）来描述路径相关的非线性：

$$\frac{dz}{dt} = A\frac{du}{dt} - \beta\left|\frac{du}{dt}\right|z - \gamma\frac{du}{dt}|z|$$

系统输出中的迟滞贡献为：

$$d_{\text{hyst}} = -D \cdot z$$

参数含义：

| 参数 | 含义 |
|------|------|
| $A$ | 初始斜率（线性化区增益） |
| $\beta, \gamma$ | 控制迟滞环的形状和宽度 |
| $D$ | 迟滞幅度缩放因子（nm） |

**稳态迟滞环幅度**：在正弦激励下，$z$ 的饱和值为 $z_\infty = \pm\sqrt{A/(\beta+\gamma)}$，对应迟滞环宽度 $2D\sqrt{A/(\beta+\gamma)}$。

### 7.3 与 FOPDT 的关系

在仿真中，Bouc-Wen 与 FOPDT 是**串联**关系，而非替代关系：

$$y_{\text{true}} = y_{\text{FOPDT}} + d_{\text{hyst}}(u) + d_{\text{noise}}$$

- FOPDT 描述**线性动态**（有限带宽、时间常数）
- Bouc-Wen 描述**非线性迟滞**（路径相关）

两者建模不同的物理机制，缺一不可。在实际控制中，ADRC 通过 ESO 将两者产生的综合效果作为"总扰动"估计并补偿，无需分别建模。

---

## 8. 整体控制架构

### 8.1 分层结构

系统采用两层控制架构：

```
设定值 r (nm)
     │
     ▼
┌─────────────────────────────┐
│  静态前馈层：LUT 反查         │  一次性消除静态非线性（迟滞）
│  V_ff = LUT⁻¹(T, r, dir)   │
└──────────────┬──────────────┘
               │ V_ff（初始电压）
               ▼
┌─────────────────────────────┐
│  动态反馈层：PID 或 ADRC     │  持续消除动态误差
│  e[k] = r - y[k]            │
│  ΔV = Controller(e[k])      │
└──────────────┬──────────────┘
               │ V_out = V_ff + ΔV（clamp 至 [V_start, V_end]）
               ▼
          Moku:Go → 压电
               │
               ▼
          µMD2 → y[k]（测量位移）
```

### 8.2 两层的职责划分

**LUT 前馈**（静态层）：

- 将初始电压直接设置到目标位移对应的近似值
- 一次性消除大部分误差（从整个行程缩短到 < 20 nm 量级）
- 无动态，不参与后续反馈循环
- 局限：对蠕变、温漂、老化不免疫

**PID/ADRC**（动态层）：

- 从 LUT 前馈的终点出发，精调到目标
- 消除 LUT 的残余误差、蠕变引起的漂移、温度偏差
- PID：依赖模型参数整定，在植物参数漂移时性能退化
- ADRC：通过 ESO 实时估计扰动，对参数漂移和未建模非线性具有内在鲁棒性

### 8.3 PID vs ADRC 的适用场景

| 场景 | 推荐 |
|------|------|
| 温度稳定、参数已知 | PID（更简单，已调好即可） |
| 宽温度范围、多次复用 | ADRC（参数漂移时仍稳定） |
| 存在明显蠕变 | ADRC（ESO 可跟踪蠕变扰动） |
| 快速动态跟踪 | ADRC + Smith（更高带宽） |
| 首次使用新器件 | 先 `--autotune` 辨识，再选控制器 |

### 8.4 系统辨识在整个架构中的位置

`identify_model()` 的输出（$K$、$\tau$、$\theta$）被三个模块共同使用：

- **IMC 整定** → 计算 PID 的 $K_p$、$K_i$
- **ADRC 初始化** → 计算 $b_0 = K/\tau$，初始化 ESO 矩阵
- **Smith Predictor** → 提供 $\theta_{\text{plant}}$ 用于延迟缓冲区长度

因此，一次辨识的结果同时服务于 PID 和 ADRC 两条控制路径，不需要针对不同控制器分别建模。

---

*本文对应实现：`control/pid.py`、`control/adrc.py`、`control/loop.py`、`tune/system_id.py`、`tune/imc.py`、`data.py`（PiezoLUT）*
